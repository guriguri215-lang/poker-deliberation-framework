# Tool contracts

<!-- Generated by scripts/generate_tool_contracts.py; do not edit manually. -->

Canonical source: `poker_deliberation.tools.contracts.tool_contracts`.

The tracked manifest contains the complete JSON Schema for every input/output model. This document is the human-readable projection of the same definitions.

## Common result contract

Version 2 results add `numeric_exactness`, `contract_version`, model/method fields, tool-specific verification metadata, and complete approximation metadata. The original three-value `exactness` remains a compatibility projection.

`approximate` success requires method, stochastic flag, seed when stochastic, positive samples or iterations, an interval or error metadata, and a stopping condition. `failed` and `unavailable` cannot carry successful numeric exactness.

Registered tool count: `21`.

## `pot_odds`

Pot odds and required equity after a bet and optional declared rake.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `PotOddsInput`
- Input fields: `pot_before_bet` (required), `opponent_bet` (required), `call_cost` (required), `expected_rake`
- Output model: `PotOddsOutput`
- Output fields: `pot_after_opponent_bet` (required), `final_pot_before_rake` (required), `expected_rake` (required), `final_pot_after_rake` (required), `required_equity` (required), `required_equity_percent` (required), `pot_odds_against` (required)
- Assumptions: ["All amounts use one currency or chip unit."]
- Preconditions: ["call_cost > 0","expected_rake < final pot before rake"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"amounts":"one caller-declared chip/currency unit","required_equity":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["formula identities","finite typed output"]
- Tolerance: {"absolute":null,"fields":["pot_after_opponent_bet","final_pot_before_rake","expected_rake","required_equity","required_equity_percent","final_pot_after_rake","pot_odds_against"],"formula":null,"kind":"ulp","rationale":"Bounded O(1) IEEE-754 arithmetic; the bound scales with result magnitude.","relative":null,"ulps":16,"unit":"output field unit"}
- Model qualifier: null

## `break_even_fold`

Zero-equity bluff break-even fold frequency.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `BreakEvenFoldInput`
- Input fields: `risk` (required), `reward` (required)
- Output model: `BreakEvenFoldOutput`
- Output fields: `risk` (required), `reward` (required), `break_even_fold_frequency` (required), `break_even_fold_percent` (required)
- Assumptions: ["Called branch has zero equity; risk and reward are incremental."]
- Preconditions: ["risk > 0","reward > 0"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"frequency":"fraction","reward":"same value unit","risk":"one value unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["frequency/percent identity","frequency lies in [0,1]"]
- Tolerance: {"absolute":null,"fields":["risk","reward","break_even_fold_frequency","break_even_fold_percent"],"formula":null,"kind":"ulp","rationale":"One division and one addition in IEEE-754 binary64.","relative":null,"ulps":16,"unit":"output field unit"}
- Model qualifier: null

## `mdf`

One-bet toy-model minimum defense frequency.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `MDFInput`
- Input fields: `pot_before_bet` (required), `bet` (required)
- Output model: `MDFOutput`
- Output fields: `minimum_defense_frequency` (required), `minimum_defense_percent` (required), `formula` (required)
- Assumptions: ["Single bet, no future action, zero-equity bluff indifference model."]
- Preconditions: ["pot_before_bet > 0","bet > 0"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"amounts":"one caller-declared unit","frequency":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["frequency/percent identity","frequency domain and formula metadata"]
- Tolerance: {"absolute":null,"fields":["minimum_defense_frequency","minimum_defense_percent"],"formula":null,"kind":"ulp","rationale":"One division and one addition in IEEE-754 binary64.","relative":null,"ulps":16,"unit":"output field unit"}
- Model qualifier: "one-bet zero-equity-bluff indifference model"

## `spr`

Stack-to-pot ratio at one supplied decision point.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `SPRInput`
- Input fields: `effective_stack` (required), `pot` (required)
- Output model: `SPROutput`
- Output fields: `spr` (required), `effective_stack` (required), `pot` (required), `formula` (required)
- Assumptions: ["Stack and pot refer to the same decision point and unit."]
- Preconditions: ["pot > 0","effective_stack >= 0"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"effective_stack":"caller unit","pot":"same unit","spr":"ratio"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["ratio identity","formula metadata"]
- Tolerance: {"absolute":null,"fields":["spr"],"formula":null,"kind":"ulp","rationale":"One IEEE-754 binary64 division.","relative":null,"ulps":8,"unit":"output field unit"}
- Model qualifier: null

## `effective_stack`

Minimum of supplied remaining stacks.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `EffectiveStackInput`
- Input fields: `stacks` (required)
- Output model: `EffectiveStackOutput`
- Output fields: `effective_stack` (required), `stacks` (required), `formula` (required)
- Assumptions: ["Stacks use one unit and identify the relevant active players."]
- Preconditions: ["at least two non-negative finite stacks"]
- Limits: {"time_complexity":"O(players)"}
- Units: {"stacks":"one caller-declared chip/currency unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["effective_stack equals min(stacks)"]
- Tolerance: {"absolute":null,"fields":["effective_stack"],"formula":null,"kind":"ulp","rationale":"The validated binary64 input value is selected without arithmetic.","relative":null,"ulps":1,"unit":"output field unit"}
- Model qualifier: null

## `rake_amount`

Declared percentage rake with an optional cap.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `RakeAmountInput`
- Input fields: `pot_total` (required), `rake_percent` (required), `rake_cap`
- Output model: `RakeAmountOutput`
- Output fields: `rake_amount` (required), `raw_rake` (required), `rake_cap`, `formula` (required)
- Assumptions: ["rake_percent uses percentage points and the declared rake timing applies."]
- Preconditions: ["0 <= rake_percent < 100","pot and cap are non-negative"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"pot/rake/cap":"one caller-declared unit","rake_percent":"percentage points"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["rake/cap identities","formula metadata"]
- Tolerance: {"absolute":null,"fields":["rake_amount","raw_rake"],"formula":null,"kind":"ulp","rationale":"One multiplication and one division plus an exact minimum selection.","relative":null,"ulps":16,"unit":"output field unit"}
- Model qualifier: null

## `raked_call_ev`

Call EV with declared final-pot rake and no future betting.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `RakedCallEVInput`
- Input fields: `equity` (required), `pot_after_bet` (required), `call_cost` (required), `rake_percent` (required), `rake_cap`
- Output model: `RakedCallEVOutput`
- Output fields: `ev` (required), `rake_amount` (required), `final_pot_after_rake` (required), `formula` (required), `model` (required)
- Assumptions: ["No future betting; supplied equity; rake is taken from the final pot."]
- Preconditions: ["equity in [0,1]","call_cost > 0","0 <= rake_percent < 100"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"EV/amounts":"one caller-declared unit","equity":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["EV/rake identities","model and formula metadata"]
- Tolerance: {"absolute":null,"fields":["ev","rake_amount","final_pot_after_rake"],"formula":null,"kind":"ulp","rationale":"Bounded straight-line binary64 arithmetic with declared inputs.","relative":null,"ulps":32,"unit":"output field unit"}
- Model qualifier: "single decision, no future betting, declared final-pot rake"

## `bluff_ev`

Single-street bluff or semi-bluff EV.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `BluffEVInput`
- Input fields: `fold_frequency` (required), `pot_before_bet` (required), `bet` (required), `equity_when_called`
- Output model: `BluffEVOutput`
- Output fields: `ev` (required), `called_branch_ev` (required), `formula` (required), `model` (required)
- Assumptions: ["Call-or-fold response; no rake or future betting."]
- Preconditions: ["frequencies in [0,1]","pot and bet > 0"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"EV/amounts":"one caller-declared unit","frequencies":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["EV branch identities","model and formula metadata"]
- Tolerance: {"absolute":null,"fields":["ev","called_branch_ev"],"formula":null,"kind":"ulp","rationale":"Bounded straight-line binary64 arithmetic.","relative":null,"ulps":32,"unit":"output field unit"}
- Model qualifier: "single-street call-or-fold response"

## `polar_river_bluff_fraction`

Polarized river bluff fraction against a bluff-catcher.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `PolarRiverInput`
- Input fields: `pot_before_bet` (required), `bet` (required)
- Output model: `PolarRiverOutput`
- Output fields: `bluff_fraction` (required), `bluff_percent` (required), `formula` (required), `model` (required)
- Assumptions: ["River; polarized range; bluff-catcher; no rake."]
- Preconditions: ["pot_before_bet > 0","bet > 0"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"amounts":"one caller-declared unit","bluff_fraction":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["fraction/percent identity","model and formula metadata"]
- Tolerance: {"absolute":null,"fields":["bluff_fraction","bluff_percent"],"formula":null,"kind":"ulp","rationale":"One division and two bounded additions/multiplications.","relative":null,"ulps":16,"unit":"output field unit"}
- Model qualifier: "polarized river bettor versus bluff-catcher"

## `bayes_update`

Bayesian posterior from supplied prior and likelihoods.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `BayesInput`
- Input fields: `prior` (required), `likelihood_given_h` (required), `likelihood_given_not_h` (required)
- Output model: `BayesOutput`
- Output fields: `posterior` (required), `evidence_probability` (required), `formula` (required)
- Assumptions: ["Prior and likelihoods are supplied assumptions, not inferred population data."]
- Preconditions: ["all probabilities in [0,1]","evidence probability > 0"]
- Limits: {"time_complexity":"O(1)"}
- Units: {"probabilities":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["posterior/evidence identity","probability domain and formula metadata"]
- Tolerance: {"absolute":null,"fields":["posterior","evidence_probability"],"formula":null,"kind":"ulp","rationale":"Bounded straight-line binary64 probability arithmetic.","relative":null,"ulps":32,"unit":"output field unit"}
- Model qualifier: "Bayes rule conditional on supplied prior and likelihoods"

## `pot_reconstruction`

Pot reconstruction from incremental contributions.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `PotReconstructionInput`
- Input fields: `starting_pot` (required), `contributions` (required)
- Output model: `PotReconstructionOutput`
- Output fields: `starting_pot` (required), `pots_after_each_contribution` (required), `final_pot` (required)
- Assumptions: ["Contributions are incremental and use one unit."]
- Preconditions: ["starting pot and every contribution are non-negative and finite"]
- Limits: {"payload_bytes":1000000,"time_complexity":"O(contributions)"}
- Units: {"pot/contributions":"one caller-declared unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["running-pot length and ordering","final-pot sum invariant"]
- Tolerance: {"absolute":null,"fields":["pots_after_each_contribution","final_pot"],"formula":"ULP bound is applied per addition and checked by exact Decimal oracle tests.","kind":"ulp","rationale":"Accumulated binary64 additions; error grows with contribution count.","relative":null,"ulps":16,"unit":"output field unit"}
- Model qualifier: null

## `combos`

Hold'em combo expansion, weights, and blocker removal.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `exact`, `floating-verified`
- Input model: `CombosInput`
- Input fields: `hand_class`, `range`, `dead_cards`
- Output model: `CombosOutput`
- Output fields: `hand_class`, `count`, `combos`, `range`, `combo_count`, `total_combo_weight`, `normalized_weights`
- Assumptions: ["Cards use canonical two-character notation."]
- Preconditions: ["exactly one of hand_class or range","range retains at least one legal combo"]
- Limits: {"dead_cards":52,"time_complexity":"O(range combos)"}
- Units: {"combo_count":"count","weights":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["combo count matches list","normalized weights sum to one"]
- Tolerance: {"absolute":null,"fields":["normalized_weights"],"formula":null,"kind":"ulp","rationale":"Only weighted-range normalization uses binary64; pure combo expansion is exact.","relative":null,"ulps":32,"unit":"output field unit"}
- Model qualifier: null

## `holdem_equity`

Heads-up Hold'em equity by bounded complete enumeration or seeded Monte Carlo.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`, `approximate`
- Input model: `HoldemEquityInput`
- Input fields: `hero_range` (required), `villain_range` (required), `board`, `dead_cards`, `game_type`, `mode`, `max_exact_evaluations`, `samples`, `seed`, `opponent_ranges`, `villain_ranges`
- Output model: `HoldemEquityOutput`
- Output fields: `method` (required), `exact` (required), `hero_equity` (required), `evaluations`, `unweighted_wins`, `unweighted_ties`, `unweighted_losses`, `range_pair_count` (required), `cards_to_come` (required), `confidence_interval_95`, `confidence_interval_method`, `samples`, `seed`, `wins`, `ties`, `losses`, `estimated_exact_evaluations`
- Assumptions: ["Heads-up only; weighted combo independence before overlap filtering."]
- Preconditions: ["legal non-overlapping ranges","board has 0, 3, 4, or 5 cards"]
- Limits: {"exact_evaluations":2000000,"monte_carlo_samples":1000000}
- Units: {"confidence_interval_95":"fraction","hero_equity":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["outcome counts equal evaluations/samples","equity and interval lie in [0,1]","seeded Monte Carlo metadata"]
- Tolerance: {"absolute":null,"fields":["hero_equity"],"formula":"Enumeration bound scales with weighted accumulation length; Monte Carlo uses its interval.","kind":"ulp","rationale":"Weighted binary64 accumulation is verified separately from sampling error.","relative":null,"ulps":128,"unit":"output field unit"}
- Model qualifier: null

## `ev_tree`

Expected value of a fully supplied finite probability tree.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `EVTreeInput`
- Input fields: `root`, `nodes` (required)
- Output model: `EVTreeOutput`
- Output fields: `root` (required), `expected_value` (required), `node_values` (required), `branches` (required)
- Assumptions: ["Branch probabilities are supplied and sum to one."]
- Preconditions: ["finite acyclic tree","each decision node has a normalized probability distribution"]
- Limits: {"depth":256,"nodes":10000}
- Units: {"payoff/expected_value":"caller value unit","probability":"fraction"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["acyclic traversal","probability normalization","node value identities"]
- Tolerance: {"absolute":null,"fields":["branch probability sums","expected_value","node_values"],"formula":"Bound scales with tree depth and branch accumulation; Fraction oracle cases are tested.","kind":"ulp","rationale":"Finite binary64 weighted sums over a bounded tree.","relative":null,"ulps":64,"unit":"output field unit"}
- Model qualifier: null

## `icm`

Independent Chip Model expected payouts.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `ICMInput`
- Input fields: `stacks` (required), `payouts` (required)
- Output model: `ICMOutput`
- Output fields: `stacks` (required), `payouts` (required), `equities` (required), `equity_sum` (required), `payable_prize_sum` (required), `sum_error` (required), `verification_tolerance` (required), `conservation_verified` (required), `zero_stack_players` (required), `warning`, `model` (required)
- Assumptions: ["ICM independence assumption; no future-game, skill, bounty, or risk model."]
- Preconditions: ["at least one positive stack","payouts non-increasing and within active places"]
- Limits: {"active_players":12,"cached_non_base_states":"sum(C(n,k), k=0..p-1)","floating_operation_upper_bound":"(2*L+4)*sum((n-k)*C(n,k), k=0..p-1)+L+p+1","listed_players":100}
- Units: {"stacks/equities/payouts":"one caller-declared prize unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["prize conservation","player symmetry","zero-stack boundary","finite recursion"]
- Tolerance: {"absolute":null,"fields":["equities","equity_sum","sum_error"],"formula":"Runtime M=(2*L+4)*sum((n-k)*C(n,k),k=0..p-1)+L+p+1 over cached non-base subset states; gamma=M*2^-53/(1-M*2^-53); ulps=max(1,ceil(4*gamma*max(1,abs(payable_prize_sum))/ulp(max(1,abs(payable_prize_sum))))). The manifest ulps value is the minimum before per-result materialization.","kind":"ulp","rationale":"Four gamma-M envelopes cover the non-cancelling DP, payable/output sums, and final subtraction; ICM remains model-conditional binary64 arithmetic.","relative":null,"ulps":1,"unit":"output field unit"}
- Model qualifier: "Independent Chip Model"

## `matrix_game`

Small two-player zero-sum matrix solution with a verified duality gap.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`, `approximate`
- Input model: `MatrixGameInput`
- Input fields: `matrix` (required), `tolerance`, `max_support_size`, `fallback_iterations`
- Output model: `MatrixGameOutput`
- Output fields: `row_strategy` (required), `column_strategy` (required), `value`, `value_estimate`, `duality_gap` (required), `row_support`, `column_support`, `row_best_response` (required), `column_best_response` (required), `method` (required), `exact_algorithm` (required), `verification_tolerance`, `support_candidates_upper_bound` (required), `iterations`, `fallback_work_estimate`, `warning`
- Assumptions: ["Two players and zero-sum payoffs."]
- Preconditions: ["finite non-empty rectangular matrix"]
- Limits: {"dimension":32,"fallback_iterations":1000000,"fictitious_work":5000000,"support_candidates":250000,"support_size":8}
- Units: {"payoff/value/duality_gap":"caller payoff unit","strategy":"probability"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["strategy normalization","support feasibility","duality gap","best-response bounds"]
- Tolerance: {"absolute":null,"fields":["strategy normalization","support feasibility","value","duality_gap"],"formula":"max(input tolerance, 64 ULPs at matrix magnitude); support residual checks use documented bounded multiples and report the applied value","kind":"caller-supplied","rationale":"Matrix conditioning is input-dependent; a universal epsilon is unsound.","relative":null,"ulps":null,"unit":"caller payoff unit"}
- Model qualifier: "finite two-player zero-sum normal-form game"

## `fixed_strategy_best_response`

Best response to a fully fixed opponent strategy in a bounded finite game.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `BestResponseInput`
- Input fields: `game` (required), `fixed_strategy` (required), `best_responder`, `max_pure_policies`
- Output model: `BestResponseOutput`
- Output fields: `best_responder` (required), `value` (required), `best_responder_value` (required), `player0_value` (required), `payoff_convention` (required), `pure_policy` (required), `evaluated_policies` (required), `policy_node_work_upper_bound` (required), `information_set_constraint_enforced` (required), `opponent_strategy_fixed` (required), `equilibrium_claim` (required)
- Assumptions: ["Perfect recall; finite acyclic tree; opponent strategy fixed at every information set."]
- Preconditions: ["two-player zero-sum payoff convention","consistent information-set actions"]
- Limits: {"depth":256,"nodes":10000,"policy_node_work":5000000,"pure_policies":1000000}
- Units: {"probability":"fraction","value/payoff":"caller payoff unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["one responder action per information set","chance and fixed opponent distributions","reported policy value","explicit non-equilibrium flag"]
- Tolerance: {"absolute":null,"fields":["chance probability sums","fixed strategy probability sums","value","player0_value"],"formula":"Bound scales with chance/opponent weighted-sum depth.","kind":"ulp","rationale":"Exhaustive policy selection is discrete; policy values use binary64 arithmetic.","relative":null,"ulps":64,"unit":"output field unit"}
- Model qualifier: "best response to one fully fixed opponent strategy"

## `hand_validator`

Canonical card, action, stack, and pot validation for declared rules.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `HandValidatorInput`
- Input fields: `game_type`, `format` (required), `table_size` (required), `small_blind` (required), `big_blind` (required), `ante`, `rake`, `players` (required), `hero_player_id`, `hero_cards`, `board`, `actions`, `tournament`, `known_ranges`, `opponent_observations`, `analysis_objective`, `tolerance`
- Output model: `HandValidatorOutput`
- Output fields: `valid` (required), `verification_tolerance` (required), `errors` (required), `warnings` (required), `final_pot` (required), `remaining_stacks` (required), `reconstructed_actions` (required), `decision_snapshots` (required), `limitations` (required)
- Assumptions: ["Action amounts are incremental; unsupported site rules remain limitations."]
- Preconditions: ["canonical hand schema","finite non-negative amounts"]
- Limits: {"board_cards":5,"hero_cards":4,"table_size":10}
- Units: {"pots/stacks/actions":"one caller-declared chip unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["card uniqueness","stack/pot reconstruction","action legality","limitation disclosure"]
- Tolerance: {"absolute":null,"fields":["pot and stack comparisons"],"formula":"default applied ULP count is max(32, 4*(actions+players)); caller override is recorded as an absolute bound","kind":"ulp","rationale":"Chip comparison precision must scale with the supplied hand rather than a global epsilon.","relative":null,"ulps":32,"unit":"caller chip unit"}
- Model qualifier: "declared canonical hand rules profile"

## `hand_pot_ledger`

Exact integer contribution, uncalled-return, side-pot, and eligibility ledger for one explicit repository-owned no-rake NLHE cash profile.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `exact-under-model`
- Input model: `HandPotLedgerInputV1`
- Input fields: `schema_version` (required), `rule_profile` (required), `hand` (required)
- Output model: `HandPotLedgerOutputV1`
- Output fields: `schema_version`, `profile_id` (required), `profile_version` (required), `supported_site` (required), `chip_unit` (required), `ledger_actions` (required), `uncalled_returns` (required), `pot_layers` (required), `player_eligibility` (required), `gross_contributions_units` (required), `net_contributions_units` (required), `remaining_stacks_units` (required), `gross_committed_units` (required), `total_returned_units` (required), `final_pot_units` (required), `starting_chips_units` (required), `conservation_verified` (required), `oracle_verified` (required), `limitations` (required)
- Assumptions: ["Action amounts use the caller-declared canonical decimal chip unit.","Only generic_nlhe_cash_no_rake_v1 version 1.0.0 and supported site none apply.","No winner assignment, payout split, rake, or hand-strength evaluation is made."]
- Preconditions: ["explicit supported profile and version","explicit zero rake","complete legal betting rounds","every monetary value is an exact bounded integer number of chip units"]
- Limits: {"actions":512,"external_execution":"not performed","ledger_units":9223372036854775807,"players":10,"supported_profiles":1}
- Units: {"all ledger, stack, return, and pot values":"input.rule_profile.chip_unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure","unsupported profile, version, site, game, format, rake, straddle, or ante shape","illegal or incomplete betting sequence","ambiguous uncalled return","non-integral or overflowing chip-unit conversion","conservation or independent-oracle mismatch"]
- Verification checks: ["integer stack and pot conservation","gross contribution equals final pot plus uncalled returns","pot layers sum to final pot","independent Fraction/integer oracle agreement"]
- Tolerance: null
- Model qualifier: "generic_nlhe_cash_no_rake_v1 version 1.0.0"

## `sensitivity`

Bounds and influence ranking over a supplied scenario grid.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `floating-verified`
- Input model: `SensitivityInput`
- Input fields: `scenarios` (required), `decision_threshold`
- Output model: `SensitivityOutput`
- Output fields: `lower_bound` (required), `upper_bound` (required), `decision_threshold` (required), `scenarios_at_or_above_threshold` (required), `influence_ranking` (required), `scenario_count` (required), `warning` (required)
- Assumptions: ["Bounds and associations apply only to the supplied grid."]
- Preconditions: ["at least one finite-valued scenario","parameters are JSON-serializable"]
- Limits: {"payload_bytes":1000000}
- Units: {"impact":"same unit","values/bounds/threshold":"caller value unit"}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["ordered bounds","scenario count","descending influence ranking"]
- Tolerance: {"absolute":null,"fields":["setting means","impact"],"formula":"Mean and spread bound scales with observations per setting.","kind":"ulp","rationale":"Grid values are finite binary64 and aggregation depth is bounded by payload size.","relative":null,"ulps":64,"unit":"output field unit"}
- Model qualifier: "caller-supplied finite scenario grid"

## `solver_status`

External solver capability discovery without a strategy result.

- Tool version: `1.0.0`
- Contract version: `2.0.0`
- Numeric exactness: `unavailable`
- Input model: `SolverStatusInput`
- Input fields: なし
- Output model: `SolverStatusOutput`
- Output fields: `status` (required), `operation` (required), `result` (required), `error` (required), `capability` (required), `unavailable` (required)
- Assumptions: []
- Preconditions: ["no input fields are accepted"]
- Limits: {"external_execution":"not performed"}
- Units: {}
- Failure modes: ["strict input schema rejection (including extra or missing fields)","documented precondition violation","hard resource limit violation","strict output schema or invariant failure"]
- Verification checks: ["unavailable status","empty result","capability.available=false"]
- Tolerance: null
- Model qualifier: null
